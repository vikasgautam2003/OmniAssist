[project]
name = "omniassist"
version = "0.1.0"
description = "A production-grade multimodal AI assistant built on tool orchestration."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.14.2",
]

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "ruff>=0.16.1",
]
